# A simple class that emulates `System.Stopwatch`.

I wrote this quick-n-dirty class to avoid my project taking a dependency on `System.dll` while still being able to play with simple throw-away Unity scripts.
